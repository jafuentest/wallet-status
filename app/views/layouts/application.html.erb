<!DOCTYPE html>
<html>
  <head>
    <title>WalletStatus</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <header class="p-2 bg-dark text-white">
      <div class="container">
        <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
          <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
            <li><%= link_to 'Balance', positions_path, class: 'nav-link px-2 text-white' %></li>
            <li><%= link_to 'Staking', staking_index_path, class: 'nav-link px-2 text-white' %></li>
            <li><%= link_to 'Transactions', transactions_path, class: 'nav-link px-2 text-white' %></li>
            <li><%= link_to 'Transactions', transactions_path, class: 'nav-link px-2 text-white' %></li>
            <li><a href="#" class="nav-link px-2 text-white">About</a></li>
          </ul>

          <div class="text-end">
            <% if signed_in? %>
              <%= link_to 'My Account', root_path, class: 'btn btn-sm btn-warning' %>
              <%= button_to 'Logout', destroy_user_session_path, class: 'btn btn-sm btn-outline-light d-inline-block border-none me-2', form_class: 'form-btn', method: :delete %>
            <% else %>
              <%= link_to 'Login', new_user_session_path, class: 'btn btn-sm btn-outline-light border-none me-2' %>
              <%= link_to 'Sign-up', new_user_registration_path, class: 'btn btn-sm btn-warning' %>
            <% end %>
          </div>
        </div>
      </div>
    </header>
    <div class="container">
      <%= bootstrap_alert unless flash.empty? %>
      <%= yield %>
    </div>
  </body>
</html>
